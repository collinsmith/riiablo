package gdx.diablo.key;

public class MappedKeyStateAdapter implements MappedKey.StateListener {
  @Override public void onPressed  (MappedKey key, @MappedKey.Keycode int keycode) {}
  @Override public void onDepressed(MappedKey key, @MappedKey.Keycode int keycode) {}
}
